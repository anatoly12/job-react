# job-reactor

## Environment variables

- `SPARKPOST_API_KEY`: SparkPost API key used for reminders.
- `SPARKPOST_RECIPIENT_EMAIL`: Optional fallback email if no recipient is present in the authenticated request.
